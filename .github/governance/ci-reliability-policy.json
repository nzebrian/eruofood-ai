{
  "_meta": {
    "artifact": "ci-reliability-policy",
    "purpose": "The bounds this repository's CI runs under: how long each job may take, how many times a network operation may be retried, which HTTP statuses and socket errors are retryable, which dependency-audit commands must go through a governed wrapper, and which controls must report a completion record. Prepared by Phase 1 of the CI Reliability & Failure-Prevention work.",
    "prepared_against_main_sha": "449ea30556eba3f58d62ce417390c8b296ac0b46",
    "why_this_file_exists": "Every control in this repository before Phase 1 protected what a gate SAYS and none protected what it COSTS. A gate can be perfectly fail-closed and still be useless if it never runs, and on 2026-09-04 that is exactly what happened twice: `Dependency audit` spent 7m01s inside one `npm audit` against a lockfile with zero advisories, and `CI \u00b7 Workflow Integrity` was CANCELLED at its ten-minute cap mid-control, silently taking four later controls with it. Neither failure was a security failure. Both were duration failures, and nothing in the repository was looking at duration.",
    "this_is_a_bound_not_a_target": "None of the timeouts below is an expectation of how long a job takes. They are the point past which a job is presumed hung. `measured_seconds` records the real observed duration so the headroom is visible and a future reader can tell a deliberate bound from a guess.",
    "validated_by": ".github/scripts/verify_ci_reliability.py, enforced inside the required `CI \u00b7 Workflow Integrity` context.",
    "mutation_tested_by": ".github/scripts/m49_ci_reliability_control.sh (policy/validator) and .github/scripts/m49_reliability_wrappers_control.sh (runtime behaviour)."
  },
  "schema": 1,
  "repository": "nzebrian/eruofood-ai",
  "job_timeout_classes": {
    "_note": "A job's declared timeout-minutes must be > 0 and <= its class maximum. There is no minimum band: a bound tighter than the class ceiling is always permitted, and a job that finishes faster than expected is not a governance problem. What is forbidden is an ABSENT bound, a zero or non-integer bound, or one above the ceiling, because those are the three routes back to GitHub's 360-minute default.",
    "fast-validation": {
      "max_minutes": 10,
      "description": "Linting, spec validation, secret scanning, dependency auditing, aggregators. Seconds of real work; the bound exists only to catch a hang."
    },
    "application-tests": {
      "max_minutes": 30,
      "description": "Language test suites with service containers. The slowest measured member is CI API quality at 9m32s."
    },
    "governance-integrity": {
      "max_minutes": 20,
      "description": "The required workflow-integrity job. Bounded separately because its worst case is COMPUTED from this file rather than estimated \u2014 see workflow_integrity_budget."
    },
    "container-build": {
      "max_minutes": 40,
      "description": "Compose build, boot, migrate, healthcheck. Image pulls and layer builds dominate and vary with registry weather."
    },
    "mobile-certification": {
      "max_minutes": 45,
      "description": "Flutter/Android/iOS toolchain provisioning plus analyze, test and build. Xcode and Gradle setup dominate."
    },
    "release-certification": {
      "max_minutes": 60,
      "description": "Tag- and dispatch-triggered release gates. Never run on a pull request, so a generous bound costs nothing routinely; it still must not be unbounded."
    },
    "deployment": {
      "max_minutes": 30,
      "description": "Image build/push and staging rollout."
    },
    "performance": {
      "max_minutes": 240,
      "description": "k6 scenarios including the optional 2h soak profile."
    }
  },
  "jobs": {
    "_note": "Every job in every workflow appears here exactly once, keyed `<workflow file>:<job id>`. A job present in a workflow but absent here fails the validator, and so does the reverse \u2014 an unlisted new job is the precise way a 360-minute default gets reintroduced. `measured_seconds` is null where the job has no observed successful run in the evidence used to prepare this file; those jobs are dispatch- or tag-triggered only.",
    "ci-api.yml:sqlite": {
      "class": "application-tests",
      "measured_seconds": 115
    },
    "ci-api.yml:quality": {
      "class": "application-tests",
      "measured_seconds": 572
    },
    "ci-web.yml:quality": {
      "class": "application-tests",
      "measured_seconds": 43
    },
    "ci-mobile.yml:quality": {
      "class": "application-tests",
      "measured_seconds": null
    },
    "ci-docker.yml:boot": {
      "class": "container-build",
      "measured_seconds": 195
    },
    "contracts.yml:validate": {
      "class": "fast-validation",
      "measured_seconds": 26
    },
    "security.yml:secret-scan": {
      "class": "fast-validation",
      "measured_seconds": 10
    },
    "security.yml:dependency-audit": {
      "class": "fast-validation",
      "measured_seconds": 12
    },
    "workflow-integrity.yml:actionlint": {
      "class": "governance-integrity",
      "measured_seconds": 104
    },
    "governance-advisory.yml:governance": {
      "class": "fast-validation",
      "measured_seconds": 23
    },
    "ga-flutter-certification.yml:android": {
      "class": "mobile-certification",
      "measured_seconds": 311
    },
    "ga-flutter-certification.yml:ios": {
      "class": "mobile-certification",
      "measured_seconds": 280
    },
    "ga-flutter-certification.yml:mobile-certification": {
      "class": "fast-validation",
      "measured_seconds": 3
    },
    "ga-docker-certification.yml:certify": {
      "class": "container-build",
      "measured_seconds": null
    },
    "ga-release-certification.yml:backend": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "ga-release-certification.yml:web": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "ga-release-certification.yml:openapi": {
      "class": "fast-validation",
      "measured_seconds": null
    },
    "ga-release-certification.yml:security-scan": {
      "class": "fast-validation",
      "measured_seconds": null
    },
    "ga-release-certification.yml:certified": {
      "class": "fast-validation",
      "measured_seconds": null
    },
    "release.yml:api": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "release.yml:web": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "release.yml:mobile": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "release.yml:contract": {
      "class": "fast-validation",
      "measured_seconds": null
    },
    "release.yml:security": {
      "class": "release-certification",
      "measured_seconds": null
    },
    "release.yml:images": {
      "class": "container-build",
      "measured_seconds": null
    },
    "release.yml:release-gate": {
      "class": "fast-validation",
      "measured_seconds": null
    },
    "staging-deploy.yml:build-images": {
      "class": "deployment",
      "measured_seconds": null
    },
    "staging-deploy.yml:deploy": {
      "class": "deployment",
      "measured_seconds": null
    },
    "performance-certification.yml:k6": {
      "class": "performance",
      "measured_seconds": null
    },
    "performance-certification.yml:soak": {
      "class": "performance",
      "measured_seconds": null
    }
  },
  "reusable_workflow_calls": {
    "_note": "Jobs that are a `uses:` call to another workflow carry no steps of their own and cannot declare timeout-minutes; the callee's jobs are bounded instead. Listed so the validator can tell a legitimately timeout-free job from an unbounded one.",
    "ids": [
      "ga-release-certification.yml:docker-clean-boot",
      "ga-release-certification.yml:flutter"
    ]
  },
  "retry": {
    "_note": "Applies to every governed network operation. Attempts are TOTAL attempts, not retries-after-the-first.",
    "max_attempts": 3,
    "max_backoff_seconds": 30,
    "retryable_http_status": [
      408,
      425,
      429,
      500,
      502,
      503,
      504
    ],
    "retryable_network_error_tokens": [
      "ETIMEDOUT",
      "ECONNRESET",
      "ECONNREFUSED",
      "EAI_AGAIN",
      "ENOTFOUND",
      "ERR_SOCKET_TIMEOUT",
      "socket timeout",
      "network timeout",
      "Connection timed out",
      "Could not resolve host",
      "Operation timed out",
      "Empty reply from server",
      "Connection reset by peer"
    ],
    "never_retryable": {
      "_note": "Retrying cannot repair any of these, so burning the budget on them delays a verdict that is already known. They fail closed on the first attempt.",
      "tokens": [
        "Invalid package tree",
        "Unexpected token",
        "Unexpected end of JSON",
        "Invalid response body",
        "ENOLOCK",
        "EUSAGE",
        "EJSONPARSE",
        "checksum",
        "does not match"
      ]
    }
  },
  "verdict_protocol": {
    "_note": "Every governed dependency audit answers with one of exactly three verdicts, in words and in an exit code. npm and composer both exit 1 for a vulnerability AND for a dead endpoint; that ambiguity is what made the 2026-09-04 incident take six minutes of log archaeology, and separating the two is the whole point.",
    "exit_pass": 0,
    "exit_vulnerable": 1,
    "exit_unavailable": 3,
    "unavailable_must_be_non_zero": true,
    "verdict_checked_before_transient": true,
    "why_that_ordering": "A run that saw a 503, retried, and then found real advisories prints both the outage banner and the finding. Matching the outage patterns first would downgrade a genuine vulnerability to 'the service was down' \u2014 the single misclassification that would turn a reliability wrapper into a security regression."
  },
  "audit_governance": {
    "_note": "Any invocation of a listed command in a workflow `run:` block must be routed through the matching approved wrapper. The audit POLICY (threshold, --locked) stays visible at the call site rather than moving inside a wrapper where it could be lowered out of sight.",
    "governed_commands": {
      "npm audit": {
        "wrapper": ".github/scripts/npm_audit_resilient.sh",
        "required_arguments": [
          "--audit-level=high"
        ]
      },
      "composer audit": {
        "wrapper": ".github/scripts/composer_audit_resilient.sh",
        "required_arguments": []
      },
      "osv-scanner": {
        "wrapper": ".github/scripts/osv_scan_resilient.sh",
        "required_arguments": [
          "--lockfile"
        ]
      }
    },
    "approved_wrappers": [
      ".github/scripts/npm_audit_resilient.sh",
      ".github/scripts/composer_audit_resilient.sh",
      ".github/scripts/osv_scan_resilient.sh"
    ],
    "_m50_10": "osv-scanner joins npm audit and composer audit under the same three-verdict contract. `dart pub outdated` was rejected as the mechanism: it reports version currency, not advisories, and a dependency can be perfectly current and still carry a CVE."
  },
  "download_governance": {
    "_note": "Tool downloads inside a required check are an external dependency like any other. Before Phase 1 these were bare `curl -fsSL` with no timeout and no retry, inside a ten-minute job: a GitHub Releases blip reddened the required integrity context, and a SLOW one consumed the budget and cancelled every control after it.",
    "wrapper": ".github/scripts/bounded_download.sh",
    "checksum_verification_required": true,
    "checksum_mismatch_is_fatal": true,
    "checksum_mismatch_is_never_retried": true,
    "workflows_requiring_governed_download": [
      ".github/workflows/workflow-integrity.yml"
    ],
    "_m50_note": "M50 Phase 1 widened this from a single workflow to every workflow. A probe confirmed a bare `curl https://...` added to security.yml was accepted by every validator, because the rule was scoped to workflow-integrity.yml alone.",
    "governed_paths": [
      ".github/workflows/*.yml"
    ],
    "non_download_curl_exemptions": [
      {
        "workflow": "ci-docker.yml",
        "match": "http://localhost:8080",
        "reason": "Health and readiness probes against the locally-booted compose stack. No artefact is fetched and no third party is involved; the bounded-download wrapper would be meaningless here. The recovery loop that uses them already carries its own 120s deadline."
      },
      {
        "workflow": "ga-docker-certification.yml",
        "match": "http://localhost:8080",
        "reason": "Same locally-booted health, readiness and smoke probes."
      },
      {
        "workflow": "staging-deploy.yml",
        "match": "${STAGING_URL}",
        "reason": "Post-deploy smoke probe against the environment just deployed, already bounded with --max-time 30. Not an artefact download."
      },
      {
        "workflow": "governance-advisory.yml",
        "match": "api.github.com",
        "reason": "Governance evidence query against the GitHub API, already bounded with --max-time 30 --retry 2 --retry-connrefused. Fetches JSON evidence, not an executable artefact, and has no checksum to verify."
      }
    ]
  },
  "npm_network_policy": {
    "_note": "npm's defaults are fetch-retries=2 with fetch-retry-maxtimeout=60000, serialised across many requests. That is how one `npm audit` consumed 7m01s on 2026-09-04. Installation and auditing get different budgets because they are different operations: an install legitimately makes hundreds of requests, an audit makes one.",
    "install": {
      "_applies_to": "npm ci, npm install",
      "fetch_retries": 2,
      "fetch_retry_mintimeout_ms": 1000,
      "fetch_retry_maxtimeout_ms": 20000,
      "fetch_timeout_ms": 60000,
      "rationale": "Retry stays enabled \u2014 a dropped request mid-install is common and cheap to retry \u2014 but the per-retry ceiling drops from 60s to 20s, bounding the tail without changing behaviour on a healthy registry."
    },
    "audit": {
      "_applies_to": "npm audit",
      "fetch_retries": 0,
      "rationale": "npm's internal retry is disabled entirely and replaced by the wrapper's own bounded, classified, observable retry. Two retry layers stacked would multiply, not add."
    },
    "publish": {
      "_applies_to": "npm publish",
      "present_in_repository": false,
      "rationale": "No workflow publishes an npm package. Recorded so that adding one is a deliberate act that must extend this policy."
    },
    "env_var_names": {
      "fetch_retries": "npm_config_fetch_retries",
      "fetch_retry_mintimeout_ms": "npm_config_fetch_retry_mintimeout",
      "fetch_retry_maxtimeout_ms": "npm_config_fetch_retry_maxtimeout",
      "fetch_timeout_ms": "npm_config_fetch_timeout"
    },
    "jobs_running_npm_install": [
      "ci-web.yml:quality",
      "contracts.yml:validate",
      "release.yml:web",
      "release.yml:security",
      "ga-release-certification.yml:web"
    ]
  },
  "composer_network_policy": {
    "_note": "`shivammathur/setup-php` exports COMPOSER_PROCESS_TIMEOUT=0 into the runner environment \u2014 zero meaning UNLIMITED. Verified verbatim in the `Dependency audit` and `Governance Advisory` job logs on 4f3f62d. A governed audit path must not inherit that.",
    "audit": {
      "process_timeout_seconds": 60,
      "rationale": "The wrapper sets COMPOSER_PROCESS_TIMEOUT explicitly and additionally imposes its own hard per-attempt timeout, so an unlimited inherited value cannot reach a governed audit."
    },
    "exit_code_semantics": {
      "0": "clean \u2014 composer audited and found nothing",
      "1": "advisories found \u2014 a real verdict",
      "100": "generic composer error \u2014 NEVER a verdict; classified UNAVAILABLE"
    }
  },
  "workflow_integrity_budget": {
    "_note": "The required integrity job runs more live external operations than any other job in the repository, and is the only one with a tight cap. Before Phase 1 nobody had multiplied these out; the cancellation on 2026-09-04 was the arithmetic asserting itself. The validator now recomputes this sum from the components below and fails if it exceeds the declared timeout.",
    "job": "workflow-integrity.yml:actionlint",
    "timeout_minutes": 20,
    "components_seconds": {
      "fixed_overhead": {
        "value": 150,
        "note": "Checkout, two extractions, actionlint over 14 files, and every structural control that makes no network call. Measured healthy total for the whole job was 104s on run 33879626208; 150 carries headroom."
      },
      "governed_downloads": {
        "count": 2,
        "worst_case_each": 51,
        "note": "3 attempts x 15s + (2s + 4s) backoff. actionlint and shellcheck."
      },
      "live_npm_audits": {
        "count": 4,
        "worst_case_each": 54,
        "note": "2 attempts x 25s + 4s backoff. M45 Part B runs two; M47 case C re-executes the entire M45 control, so it runs two more."
      },
      "live_composer_audits": {
        "count": 4,
        "worst_case_each": 34,
        "note": "2 attempts x 15s + 4s backoff. Same doubling as npm."
      },
      "baseline_git_fetch": {
        "count": 2,
        "worst_case_each": 34,
        "note": "2 attempts x 15s + 4s backoff, once per M45 control execution."
      }
    },
    "computed_worst_case_seconds": 672,
    "headroom_seconds": 528,
    "why_not_a_separate_job": "Splitting the live dependency-audit controls into their own job was evaluated and REJECTED. A new job is a new status-check context, and a new context is not in the ruleset, so the live audit controls would silently stop being required \u2014 a bypass created by a change made in the name of reliability. Bounding every operation and computing the total achieves the same protection without detaching anything from the required context."
  },
  "control_manifest": {
    "_note": "A job's conclusion tells you whether it finished, never which of its controls ran. When the integrity job was cancelled on 2026-09-04, four controls produced no result and reconstructing which ones required reading step timings by hand. Each enforced control now writes a completion record, and a final step asserts the recorded set is exactly the mandatory set.",
    "directory": ".ci-control-manifest",
    "runner": ".github/scripts/run_control.sh",
    "verifier": ".github/scripts/verify_control_manifest.py",
    "verdicts": [
      "PASS",
      "FAIL",
      "UNAVAILABLE",
      "NOT_RUN"
    ],
    "not_run_fails_the_job": true,
    "mandatory_controls": [
      "workflow_integrity_negative_control",
      "m35_docker_certification_guard",
      "m35_docker_certification_negative_control",
      "m31_platform_negative_control",
      "m37_governance_advisory_control",
      "verify_deployment_safety",
      "m44_deployment_safety_control",
      "verify_dependency_audit_gate",
      "verify_ci_reliability",
      "m49_ci_reliability_control",
      "m49_reliability_wrappers_control",
      "verify_workflow_privilege",
      "m46_workflow_privilege_control",
      "m45_dependency_audit_control",
      "m47_control_integrity_control",
      "m48_npm_audit_resilience_control",
      "m36_ci_enforcement_control"
    ]
  },
  "masking_governance": {
    "_note": "M50 Phase 1. Before this, masking was checked in exactly two workflows (security.yml via the audit gate, staging-deploy.yml via deployment safety) and in five reliability scripts. Twelve of fourteen workflows were unchecked, and a probe confirmed that adding `|| true` to a GA certification command was accepted by every validator. The rule is now repository-wide.",
    "_why_not_a_blanket_ban": "Some `||` constructs are correct and removing them would break working jobs. `grep` exits 1 when it matches nothing, so `grep ... || true` is the idiom for 'collect matches, there may be none'. Teardown must run whether or not the thing being torn down came up. And a reporting step that runs after a gate has already decided cannot change that decision. So the ban is on UNEXPLAINED masking: every exemption below names a workflow, a step and a reason, and a construct that matches none of them fails the check.",
    "_no_wildcards": "There is deliberately no way to exempt a whole workflow, a whole construct, or a path prefix. An exemption is (workflow, step, construct, reason) or it does not exist. A future maintainer adding a mask must say where and why.",
    "governed_paths": [
      ".github/workflows/*.yml"
    ],
    "forbidden_constructs": [
      "|| true",
      "|| :",
      "|| echo",
      "continue-on-error",
      "set +e",
      "if: always()"
    ],
    "exemptions": [
      {
        "workflow": "ci-api.yml",
        "step": "Upload coverage",
        "construct": "if: always()",
        "reason": "Artifact upload after the test gate. Runs so coverage is retrievable from a failed run; cannot change the job's conclusion."
      },
      {
        "workflow": "ci-docker.yml",
        "step": "Wait for health (postgres, redis, api)",
        "construct": "|| true",
        "reason": "grep no-match idiom: `grep -E 'starting|unhealthy' || true` returns empty when every container is healthy, which is the success case."
      },
      {
        "workflow": "ci-docker.yml",
        "step": "Teardown",
        "construct": "if: always()",
        "reason": "`docker compose down -v` after the gate. Must run on failure or the runner leaks volumes; cannot change the conclusion."
      },
      {
        "workflow": "ga-docker-certification.yml",
        "step": "Ensure NO cached volumes/state (clean env)",
        "construct": "|| true",
        "reason": "Pre-flight teardown of a stack that may not exist yet. Failure here means there was nothing to remove, which is the desired starting state."
      },
      {
        "workflow": "ga-docker-certification.yml",
        "step": "Wait for service health",
        "construct": "|| true",
        "reason": "grep no-match idiom, as in ci-docker.yml."
      },
      {
        "workflow": "ga-docker-certification.yml",
        "step": "Capture container logs on failure",
        "construct": "|| true",
        "reason": "Diagnostic log collection on an already-failing run. A container that died may have no logs to collect; failing here would replace the real error with a log-collection error."
      },
      {
        "workflow": "ga-docker-certification.yml",
        "step": "Clean teardown (remove volumes \u2014 no state persists)",
        "construct": "if: always()",
        "reason": "Teardown after the gate, as in ci-docker.yml."
      },
      {
        "workflow": "ga-flutter-certification.yml",
        "job": "mobile-certification",
        "construct": "if: always()",
        "reason": "M33, and load-bearing. A bare `needs:` SKIPS the aggregator when a dependency fails, and GitHub treats a skipped required check as pending rather than failed. `always()` makes it run; the job then compares needs.android.result and needs.ios.result against the literal 'success' and fails on anything else. Removing this would reopen the M29-A pending trap."
      },
      {
        "workflow": "governance-advisory.yml",
        "step": "Publish the verdict",
        "construct": "if: always()",
        "reason": "Reporting step after the ratchet has already decided. Built from JSON the ratchet wrote; scrapes no console text and cannot alter the conclusion."
      },
      {
        "workflow": "governance-advisory.yml",
        "step": "Upload evidence",
        "construct": "if: always()",
        "reason": "Evidence artifact upload after the gate."
      },
      {
        "workflow": "performance-certification.yml",
        "step": "Human-readable report row",
        "construct": "if: always()",
        "reason": "Summary rendered from the k6 summary JSON after the run. Guarded by `if [ -f ... ]`; reports, gates nothing."
      },
      {
        "workflow": "performance-certification.yml",
        "step": "Upload machine-readable results",
        "construct": "if: always()",
        "reason": "k6 result artifact upload after the run."
      },
      {
        "workflow": "performance-certification.yml",
        "step": "<unnamed>",
        "construct": "if: always()",
        "reason": "Soak-profile result artifact upload after the run. The step is unnamed in the workflow; matched by its position as the upload-artifact step of the soak job."
      }
    ],
    "deferred_defects": [],
    "_deferred_defects_note": "Empty, and that is the desired state. The only entry this list ever held was M50-13 \u2014 the masked `db:seed` in the two Docker certification workflows \u2014 and it was a record of a real defect awaiting a fix, never an exemption. The fix landed: apps/api/database/seeders/DatabaseSeeder.php now exists, both masks are gone, and the records went with them. A future deferral is added here the same way: named, reported by the validator on every run, and its silent disappearance is itself a failing check."
  },
  "concurrency_governance": {
    "_note": "M50 Phase 1. ci-docker.yml was the only workflow of fourteen with no concurrency group, so a superseded run kept building images and booting a compose stack nobody was waiting for. Every workflow's concurrency behaviour is now stated here, so the next one to go missing is a policy violation rather than something nobody notices.",
    "group_required": true,
    "cancel_in_progress_default": true,
    "cancel_in_progress_exemptions": [
      {
        "workflow": "release.yml",
        "reason": "Tag-triggered production release. Cancelling a half-finished release run would leave images built and a release gate un-evaluated; two tags are independent releases and must both complete."
      },
      {
        "workflow": "staging-deploy.yml",
        "reason": "Deploys to a live staging environment. Cancelling mid-rollout leaves the cluster in a partially-updated state; the single `staging-deploy` group serialises deployments instead."
      },
      {
        "workflow": "performance-certification.yml",
        "reason": "Long-running k6 scenarios including a 2h soak. A superseding run must not discard hours of in-flight measurement."
      }
    ]
  },
  "network_policies": {
    "_note": "M50 Phase 1. npm was the only ecosystem with a network policy; composer, flutter and apt had none, and setup-php exports COMPOSER_PROCESS_TIMEOUT=0 (unlimited) into every job. Each entry names how the bound is expressed, because the mechanisms genuinely differ: composer has an env var, apt has command options, and pub has neither \u2014 so its bound is a step timeout.",
    "composer_install": {
      "mechanism": "job_env",
      "required_env": {
        "COMPOSER_PROCESS_TIMEOUT": "300"
      },
      "detect": "composer install",
      "rationale": "setup-php exports COMPOSER_PROCESS_TIMEOUT=0 \u2014 zero meaning UNLIMITED, verified verbatim in the Dependency audit and Governance Advisory job logs. 300s is generous for a cached install and finite for a stalled one."
    },
    "flutter_pub_get": {
      "mechanism": "step_timeout_minutes",
      "required_step_timeout_minutes_max": 10,
      "detect": "flutter pub get",
      "rationale": "Dart's pub exposes no network-timeout or retry environment variable, so an env-based bound would be theatre. The honest bound is a step-level timeout: native to Actions, explicit, and verifiable."
    },
    "apt_get_install": {
      "mechanism": "command_options",
      "required_options": [
        "Acquire::Retries=3",
        "Acquire::http::Timeout=30"
      ],
      "detect_regex": "apt-get\\b[^\\n]*\\binstall\\b",
      "rationale": "apt has native retry and timeout options. Note the detector is a regex, not a substring: once the options are inserted, `apt-get install` is no longer contiguous text."
    },
    "_docker_deferred": "Docker registry pull/push bounds and the production Dockerfiles' own retry logic are M50 Phase 2. Phase 1 is forbidden to modify infra/**."
  },
  "action_pinning": {
    "_note": "M50-01. Every external action was referenced by a mutable tag \u2014 62 of 62, none pinned. A tag is a pointer the action's owner can move, so `actions/checkout@v4` is a promise to run whatever that repository decides v4 means on the morning CI happens to run. Twenty of the sixty-two were third-party, and four of those sit in staging-deploy.yml, the one workflow holding a registry token and a kubeconfig. Pinning does not make a supply-chain compromise impossible; it makes it an event somebody has to commit rather than one that arrives on its own.",
    "_refresh": "Dependabot's github-actions ecosystem is already configured for / on a weekly interval, and it understands SHA pins: it bumps the SHA and rewrites the trailing version comment. Pinning is therefore not a maintenance dead end \u2014 it is the same weekly pull request, carrying a SHA instead of nothing.",
    "_comment_contract": "Every pinned reference carries a trailing `# <version>` so a human reading the diff can see what moved.",
    "pinned_actions": {
      "actions/checkout": {
        "sha": "11d5960a326750d5838078e36cf38b85af677262",
        "version": "v4.4.0"
      },
      "actions/setup-node": {
        "sha": "49933ea5288caeca8642d1e84afbd3f7d6820020",
        "version": "v4.4.0"
      },
      "actions/setup-java": {
        "sha": "cf277c60eb25467037889841efdb72551f06f6c3",
        "version": "v4.9.1"
      },
      "actions/upload-artifact": {
        "sha": "ea165f8d65b6e75b540449e92b4886f43607fa02",
        "version": "v4.6.2"
      },
      "actions/cache": {
        "sha": "0057852bfaa89a56745cba8c7296529d2fc39830",
        "version": "v4.3.0"
      },
      "shivammathur/setup-php": {
        "sha": "f3e473d116dcccaddc5834248c87452386958240",
        "version": "2.37.2"
      },
      "subosito/flutter-action": {
        "sha": "1a449444c387b1966244ae4d4f8c696479add0b2",
        "version": "v2.23.0"
      },
      "gitleaks/gitleaks-action": {
        "sha": "ff98106e4c7b2bc287b24eaf42907196329070c7",
        "version": "v2.3.9"
      },
      "docker/setup-buildx-action": {
        "sha": "8d2750c68a42422c14e847fe6c8ac0403b4cbd6f",
        "version": "v3.12.0"
      },
      "docker/login-action": {
        "sha": "c94ce9fb468520275223c153574b00df6fe4bcc9",
        "version": "v3.7.0"
      },
      "docker/build-push-action": {
        "sha": "10e90e3645eae34f1e60eeb005ba3a3d33f178e8",
        "version": "v6.19.2"
      }
    }
  },
  "image_tag_policy": {
    "_note": "M50-18(b). `minio/minio:latest` sat in docker-compose.yml, which both Docker certification gates load on every run \u2014 so an upstream MinIO release could turn a green certification red with no change to this repository. That is the same third-party-uptime coupling M48 removed from npm audit, arriving through a different door.",
    "_why_not_digests_here": "Digest-pinning every base image is M50-18(a) and is deliberately NOT done here. MinIO never re-pushes a RELEASE.* tag and mailpit never re-pushes a vX.Y.Z tag, so a named release is already immutable in practice for these two, and a digest would add a refresh burden without adding a guarantee.",
    "governed_files": [
      "docker-compose.yml",
      "docker-compose.ci.yml",
      "docker-compose.override.yml",
      "docker-compose.staging.yml",
      "infra/docker/node/Dockerfile",
      "infra/docker/php/Dockerfile"
    ],
    "forbidden_tag_suffixes": [
      ":latest",
      ":edge",
      ":main",
      ":master"
    ],
    "_forbidden_rationale": "A tag whose meaning is 'whatever is newest' cannot be reproduced, so a build that used it cannot be explained afterwards.",
    "deferred": [
      {
        "finding": "M50-18(a)",
        "subject": "digest-pinning the seven floating base-image tags",
        "images": [
          "node:22-alpine",
          "nginx:1.27-alpine",
          "php:8.4-fpm-alpine",
          "pgvector/pgvector:pg16",
          "redis:7",
          "redis:7-alpine",
          "postgres:16"
        ],
        "status": "DEFERRED \u2014 no verified refresh mechanism",
        "reason": "The authorisation was explicit: digest-pin only if a maintainable refresh mechanism is established SAFELY, and do not force it otherwise. .github/dependabot.yml configures the docker ecosystem for /infra/docker/php only \u2014 not /infra/docker/node, and not the compose files at the repository root, which is where four of these seven live. Whether Dependabot's docker ecosystem would track compose services at `directory: \"/\"` could not be verified from the implementation environment, and a pinned digest nobody bumps is WORSE than a floating minor tag: the floating tag at least receives upstream security patches, while a stale digest silently freezes them. Pinning without a proven bump path would trade a small reproducibility problem for a real patch-latency one.",
        "what_would_unblock_it": "Extend .github/dependabot.yml to /infra/docker/node and to the compose files, merge that alone, and observe one Dependabot cycle actually opening a digest bump. Then pin, with evidence.",
        "not_deferred": "M50-18(b) \u2014 the two `:latest` references \u2014 is FIXED here, and is enforced by image_tag.no_mutable."
      }
    ]
  },
  "build_file_governance": {
    "_note": "M50-02. Masking governance stopped at `.github/workflows/*.yml`, and that boundary is exactly why `npm ci || npm install` survived in the production web image for as long as it did. `npm ci` installs the locked tree and fails when the lock and package.json disagree; `npm install` resolves the ranges afresh and rewrites the lock. The fallback turned 'this lockfile is stale, stop' into 'ship whatever the registry has today', in an image staging-deploy.yml pushes and deploys \u2014 while the required Dependency audit gate went on auditing the lockfile the image might not have used.",
    "governed_paths": [
      "infra/docker/node/Dockerfile",
      "infra/docker/php/Dockerfile"
    ],
    "forbidden_constructs": [
      {
        "pattern": "npm ci\\s*\\|\\|",
        "reason": "an unlocked fallback behind a locked install"
      },
      {
        "pattern": "npm install(?!.*--package-lock-only)",
        "reason": "range-resolving install in an image build; use npm ci"
      },
      {
        "pattern": "composer install.*\\|\\|\\s*true",
        "reason": "a masked dependency install"
      },
      {
        "pattern": "\\|\\|\\s*true",
        "reason": "failure masking in a build file"
      }
    ],
    "_exemptions_note": "There are none, and adding one should be hard: an image build has no equivalent of a report step whose failure cannot change a verdict.",
    "exemptions": [
      {
        "file": "infra/docker/php/Dockerfile",
        "construct": "\\|\\|\\s*true",
        "line_contains": "addgroup -g ${GID} app",
        "reason": "Create-if-absent, not failure masking. `addgroup` exits non-zero when the group already exists \u2014 which it does on any base image that ships a matching GID \u2014 and the next line depends on the group existing, not on this command having created it. A failure that actually matters still surfaces there."
      },
      {
        "file": "infra/docker/php/Dockerfile",
        "construct": "\\|\\|\\s*true",
        "line_contains": "adduser -D -u ${UID} -G app app",
        "reason": "Same create-if-absent case for the user. If the user genuinely could not be created, the `chown -R app:app` later in the same image fails and the build fails with it, so this masks nothing that is not caught downstream."
      }
    ]
  },
  "lockfile_governance": {
    "_note": "M50-09. packages/api-contracts had no lockfile at all and installed with `npm install --no-audit`, so every run re-resolved `@redocly/cli: ^1.25.0` and `openapi-typescript: ^7.4.1` from the registry \u2014 behind a required context whose job is to detect DRIFT in generated types. The generator could change between two runs of the same commit, and the drift it reported would say nothing about the spec.",
    "required_lockfiles": [
      {
        "directory": "apps/web",
        "lockfile": "apps/web/package-lock.json",
        "install_command": "npm ci",
        "workflows": [
          "ci-web.yml",
          "release.yml",
          "ga-release-certification.yml"
        ]
      },
      {
        "directory": "packages/api-contracts",
        "lockfile": "packages/api-contracts/package-lock.json",
        "install_command": "npm ci",
        "workflows": [
          "contracts.yml"
        ]
      },
      {
        "directory": "apps/api",
        "lockfile": "apps/api/composer.lock",
        "install_command": "composer install",
        "workflows": []
      },
      {
        "directory": "apps/mobile",
        "lockfile": "apps/mobile/pubspec.lock",
        "install_command": "flutter pub get",
        "workflows": []
      }
    ],
    "forbidden_install_commands": [
      "npm install"
    ],
    "_forbidden_note": "In a workflow. `npm install --package-lock-only` is how a lockfile is generated locally and is not an install."
  }
}
