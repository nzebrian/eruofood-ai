# @eruofood/ui-kit

Shared React design-system package (buttons, inputs, layout primitives, tokens)
consumed by the web app — and, where practical, mirrored by the Flutter theme.

Empty in the foundation phase. Components are extracted here as shared UI
patterns emerge, so styling stays consistent and DRY across features.
