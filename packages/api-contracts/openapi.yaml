openapi: 3.1.0
info:
  title: EruoFood AI API
  version: 0.6.0
  description: >
    Contract-first REST API for EruoFood AI. This specification is the single
    source of truth for the API surface; TypeScript (web) and Dart (mobile)
    types are generated from it so clients and server never drift
    (MASTER_PLAN.md §6.1). Milestone 5 adds the Nutrition, Health &
    Personalisation endpoints (profile, calculators, diary, meal plans,
    progress and AI recommendations).
  license:
    name: Proprietary
servers:
  - url: http://localhost:8080/api/v1
    description: Local (through the Nginx edge)
  - url: https://api.eruofood.ai/v1
    description: Production

tags:
  - name: Platform
    description: Operational and health endpoints.
  - name: Authentication
    description: Registration, login, tokens, verification, password reset.
  - name: Two-Factor
    description: TOTP two-factor enrolment and management.
  - name: Profile
    description: The authenticated user's own account.
  - name: Sessions
    description: Active session (refresh-token) management.
  - name: Admin
    description: Administrative user & role management (RBAC).
  - name: Foods
    description: Nigerian food database — browse, search, detail.
  - name: Recipes
    description: Recipes — browse, detail, CRUD, reviews, favourites, versions.
  - name: Catalog Admin
    description: Admin management of foods, categories, and ingredients.
  - name: AI
    description: AI-powered recipe generation, cooking assistant, chat history and usage.
  - name: AI Admin
    description: Admin management of the versioned prompt system (RBAC).
  - name: Nutrition
    description: Health profiles, calculators, food diary, meal plans, progress and AI personalisation.
  - name: Nutrition Admin
    description: Admin management of the nutrition database (RBAC).
  - name: Marketplace
    description: Restaurants/vendors, menus, cart, checkout, orders, delivery and riders.
  - name: Marketplace Admin
    description: Admin vendor verification & moderation (RBAC).

security:
  - bearerAuth: []

paths:
  /health:
    get:
      tags: [Platform]
      operationId: getHealth
      summary: Platform health/status
      security: []
      responses:
        "200":
          description: The platform is responding.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/SystemStatus" }

  # ---- Authentication ------------------------------------------------------
  /auth/register:
    post:
      tags: [Authentication]
      operationId: register
      summary: Register a new user (email + password)
      description: Creates an account, sends a verification email, and returns tokens.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RegisterRequest" }
      responses:
        "201": { $ref: "#/components/responses/AuthSuccess" }
        "409": { $ref: "#/components/responses/Error" }
        "422": { $ref: "#/components/responses/Error" }

  /auth/login:
    post:
      tags: [Authentication]
      operationId: login
      summary: Log in with email + password
      description: Returns tokens, or a two-factor challenge if 2FA is enabled.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/LoginRequest" }
      responses:
        "200": { $ref: "#/components/responses/AuthSuccessOrChallenge" }
        "401": { $ref: "#/components/responses/Error" }

  /auth/login/two-factor:
    post:
      tags: [Authentication]
      operationId: loginTwoFactor
      summary: Complete a two-factor login challenge
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [challenge_token, code]
              properties:
                challenge_token: { type: string }
                code: { type: string, example: "123456" }
      responses:
        "200": { $ref: "#/components/responses/AuthSuccess" }
        "401": { $ref: "#/components/responses/Error" }

  /auth/login/social:
    post:
      tags: [Authentication]
      operationId: loginSocial
      summary: Log in with a social provider (Google; Apple architecture-ready)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [provider, id_token]
              properties:
                provider: { type: string, enum: [google, apple] }
                id_token: { type: string }
      responses:
        "200": { $ref: "#/components/responses/AuthSuccess" }
        "401": { $ref: "#/components/responses/Error" }

  /auth/refresh:
    post:
      tags: [Authentication]
      operationId: refresh
      summary: Exchange a refresh token for a new access token (rotates the refresh token)
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token: { type: string }
      responses:
        "200":
          description: New token pair.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/Tokens" }
        "401": { $ref: "#/components/responses/Error" }

  /auth/logout:
    post:
      tags: [Authentication]
      operationId: logout
      summary: Revoke the current refresh token (end the session)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [refresh_token]
              properties:
                refresh_token: { type: string }
      responses:
        "204": { description: Logged out. }

  /auth/email/verify:
    post:
      tags: [Authentication]
      operationId: verifyEmail
      summary: Verify an email address using the emailed token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [uid, token]
              properties:
                uid: { type: string, format: uuid }
                token: { type: string }
      responses:
        "200": { $ref: "#/components/responses/Message" }

  /auth/email/resend:
    post:
      tags: [Authentication]
      operationId: resendVerification
      summary: Resend the email-verification message
      responses:
        "202": { $ref: "#/components/responses/Message" }

  /auth/password/forgot:
    post:
      tags: [Authentication]
      operationId: forgotPassword
      summary: Request a password-reset link
      description: Always returns 202 to avoid account enumeration.
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email]
              properties:
                email: { type: string, format: email }
      responses:
        "202": { $ref: "#/components/responses/Message" }

  /auth/password/reset:
    post:
      tags: [Authentication]
      operationId: resetPassword
      summary: Reset the password using the emailed token
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ResetPasswordRequest" }
      responses:
        "200": { $ref: "#/components/responses/Message" }
        "401": { $ref: "#/components/responses/Error" }

  # ---- Profile -------------------------------------------------------------
  /me:
    get:
      tags: [Profile]
      operationId: getProfile
      summary: Get the authenticated user's profile
      responses:
        "200": { $ref: "#/components/responses/User" }
        "401": { $ref: "#/components/responses/Error" }
    put:
      tags: [Profile]
      operationId: updateProfile
      summary: Update name and phone
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name]
              properties:
                name: { type: string }
                phone: { type: string, nullable: true, example: "+2348012345678" }
      responses:
        "200": { $ref: "#/components/responses/User" }
    delete:
      tags: [Profile]
      operationId: deleteAccount
      summary: Soft-delete the authenticated account
      responses:
        "204": { description: Account deleted. }

  /me/password:
    put:
      tags: [Profile]
      operationId: changePassword
      summary: Change the password (requires the current password)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, password, password_confirmation]
              properties:
                current_password: { type: string }
                password: { type: string }
                password_confirmation: { type: string }
      responses:
        "200": { $ref: "#/components/responses/Message" }
        "401": { $ref: "#/components/responses/Error" }

  /me/preferences:
    put:
      tags: [Profile]
      operationId: updatePreferences
      summary: Update the user's preferences (free-form JSON object)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [preferences]
              properties:
                preferences: { type: object, additionalProperties: true }
      responses:
        "200": { $ref: "#/components/responses/User" }

  /me/avatar:
    post:
      tags: [Profile]
      operationId: uploadAvatar
      summary: Upload a profile photo
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [avatar]
              properties:
                avatar: { type: string, format: binary }
      responses:
        "200": { $ref: "#/components/responses/User" }

  /me/two-factor/enable:
    post:
      tags: [Two-Factor]
      operationId: enableTwoFactor
      summary: Begin 2FA enrolment (returns secret, provisioning URI, recovery codes)
      responses:
        "200":
          description: Enrolment details.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { $ref: "#/components/schemas/TwoFactorEnrollment" }

  /me/two-factor/confirm:
    post:
      tags: [Two-Factor]
      operationId: confirmTwoFactor
      summary: Confirm 2FA with the first TOTP code
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code: { type: string }
      responses:
        "200": { $ref: "#/components/responses/Message" }
        "401": { $ref: "#/components/responses/Error" }

  /me/two-factor:
    delete:
      tags: [Two-Factor]
      operationId: disableTwoFactor
      summary: Disable 2FA (requires the account password)
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [password]
              properties:
                password: { type: string }
      responses:
        "200": { $ref: "#/components/responses/Message" }

  # ---- Sessions ------------------------------------------------------------
  /me/sessions:
    get:
      tags: [Sessions]
      operationId: listSessions
      summary: List active sessions (devices)
      responses:
        "200":
          description: Active sessions.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/Session" }

  /me/sessions/{sessionId}:
    delete:
      tags: [Sessions]
      operationId: revokeSession
      summary: Revoke a specific session
      parameters:
        - name: sessionId
          in: path
          required: true
          schema: { type: string, format: uuid }
      responses:
        "204": { description: Session revoked. }

  # ---- Admin ---------------------------------------------------------------
  /admin/users:
    get:
      tags: [Admin]
      operationId: listUsers
      summary: List users (admin only)
      parameters:
        - { name: page, in: query, schema: { type: integer, default: 1 } }
        - { name: per_page, in: query, schema: { type: integer, default: 20 } }
      responses:
        "200":
          description: Paginated users.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items: { $ref: "#/components/schemas/User" }
                  meta: { $ref: "#/components/schemas/PageMeta" }
        "403": { $ref: "#/components/responses/Error" }

  /admin/users/{userId}/roles:
    post:
      tags: [Admin]
      operationId: assignRole
      summary: Assign a role to a user (admin only)
      parameters:
        - { name: userId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role: { type: string, enum: [admin, moderator, user] }
      responses:
        "200": { $ref: "#/components/responses/User" }
    delete:
      tags: [Admin]
      operationId: revokeRole
      summary: Revoke a role from a user (admin only)
      parameters:
        - { name: userId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [role]
              properties:
                role: { type: string, enum: [admin, moderator, user] }
      responses:
        "200": { $ref: "#/components/responses/User" }

  # ---- Catalog: categories & ingredients (public) --------------------------
  /categories:
    get:
      tags: [Foods]
      operationId: listCategories
      summary: List food categories
      security: []
      responses:
        "200":
          description: Categories.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/Category" } }

  /ingredients:
    get:
      tags: [Foods]
      operationId: searchIngredients
      summary: Search ingredients
      security: []
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: page, in: query, schema: { type: integer, default: 1 } }
      responses:
        "200":
          description: Ingredients.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/Ingredient" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }

  # ---- Catalog: foods (public) --------------------------------------------
  /foods:
    get:
      tags: [Foods]
      operationId: searchFoods
      summary: Browse/search the food database
      security: []
      parameters:
        - { name: q, in: query, schema: { type: string }, description: Free-text name search. }
        - { name: category_id, in: query, schema: { type: string, format: uuid } }
        - { name: region, in: query, schema: { type: string, enum: [north_central, north_east, north_west, south_east, south_south, south_west, nationwide] } }
        - { name: state, in: query, schema: { type: string }, description: Nigerian state where the food is popular. }
        - { name: tag, in: query, schema: { type: string } }
        - { name: sort, in: query, schema: { type: string, enum: [name, recent] } }
        - { name: page, in: query, schema: { type: integer, default: 1 } }
      responses:
        "200":
          description: Paginated foods (summary shape).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/FoodSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }

  /foods/{slug}:
    get:
      tags: [Foods]
      operationId: getFood
      summary: Food detail by slug
      security: []
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Food detail.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Food" } }
        "404": { $ref: "#/components/responses/Error" }

  /foods/{foodId}/recipes:
    get:
      tags: [Recipes]
      operationId: recipesForFood
      summary: Recipes for a food
      security: []
      parameters:
        - { name: foodId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Paginated recipes (summary).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/RecipeSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }

  # ---- Catalog: recipes ----------------------------------------------------
  /recipes:
    get:
      tags: [Recipes]
      operationId: searchRecipes
      summary: Browse/search recipes
      security: []
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: food_id, in: query, schema: { type: string, format: uuid } }
        - { name: difficulty, in: query, schema: { type: string, enum: [easy, medium, hard] } }
        - { name: tag, in: query, schema: { type: string } }
        - { name: max_minutes, in: query, schema: { type: integer } }
        - { name: sort, in: query, schema: { type: string, enum: [recent, rating, quick] } }
      responses:
        "200":
          description: Paginated recipes (summary).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/RecipeSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
    post:
      tags: [Recipes]
      operationId: createRecipe
      summary: Create a recipe (authenticated)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecipeInput" }
      responses:
        "201":
          description: Created recipe.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Recipe" } }

  /recipes/{slug}:
    get:
      tags: [Recipes]
      operationId: getRecipe
      summary: Recipe detail by slug
      security: []
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: Recipe detail.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Recipe" } }
        "404": { $ref: "#/components/responses/Error" }

  /recipes/{id}:
    put:
      tags: [Recipes]
      operationId: updateRecipe
      summary: Update a recipe (owner or admin; bumps version)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/RecipeInput" }
      responses:
        "200":
          description: Updated recipe.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Recipe" } }
        "403": { $ref: "#/components/responses/Error" }
    delete:
      tags: [Recipes]
      operationId: deleteRecipe
      summary: Delete a recipe (owner or admin)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }

  /recipes/{id}/related:
    get:
      tags: [Recipes]
      operationId: relatedRecipes
      summary: Related recipes
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Related recipes (summary).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/RecipeSummary" } }

  /recipes/{id}/reviews:
    get:
      tags: [Recipes]
      operationId: listRecipeReviews
      summary: List a recipe's reviews
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Paginated reviews.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/RecipeReview" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
    post:
      tags: [Recipes]
      operationId: submitRecipeReview
      summary: Submit/update a rating & review (authenticated)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating: { type: integer, minimum: 1, maximum: 5 }
                comment: { type: string, nullable: true }
      responses:
        "201":
          description: Saved review.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/RecipeReview" } }

  /recipes/{id}/versions:
    get:
      tags: [Recipes]
      operationId: recipeVersions
      summary: Recipe version history
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Version snapshots (newest first).
          content:
            application/json:
              schema:
                type: object
                properties:
                  data:
                    type: array
                    items:
                      type: object
                      properties:
                        version: { type: integer }
                        snapshot: { type: object, additionalProperties: true }
                        created_at: { type: string, format: date-time }

  # ---- Favourites (authenticated) -----------------------------------------
  /me/favourites:
    get:
      tags: [Recipes]
      operationId: listFavourites
      summary: List the user's favourite recipes
      responses:
        "200":
          description: Paginated favourite recipes.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/RecipeSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }

  /me/favourites/{recipeId}:
    post:
      tags: [Recipes]
      operationId: addFavourite
      summary: Favourite a recipe
      parameters:
        - { name: recipeId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "201": { $ref: "#/components/responses/Message" }
    delete:
      tags: [Recipes]
      operationId: removeFavourite
      summary: Un-favourite a recipe
      parameters:
        - { name: recipeId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Removed. }

  # ---- Catalog admin (role:admin) -----------------------------------------
  /admin/foods:
    post:
      tags: [Catalog Admin]
      operationId: adminCreateFood
      summary: Create a food
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/FoodInput" }
      responses:
        "201":
          description: Created food.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Food" } }

  /admin/foods/{id}/publish:
    post:
      tags: [Catalog Admin]
      operationId: adminPublishFood
      summary: Publish a food
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Published food.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Food" } }

  /admin/foods/{id}/images:
    post:
      tags: [Catalog Admin]
      operationId: adminUploadFoodImage
      summary: Upload a food image
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          multipart/form-data:
            schema:
              type: object
              required: [image]
              properties:
                image: { type: string, format: binary }
      responses:
        "201":
          description: Food with the new image.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Food" } }

  /admin/categories:
    post:
      tags: [Catalog Admin]
      operationId: adminCreateCategory
      summary: Create a category
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, type]
              properties:
                name: { type: string }
                type: { type: string, enum: [soup, swallow, rice, protein, snack, street_food, drink, dessert, breakfast, side_dish] }
                description: { type: string, nullable: true }
                sort_order: { type: integer }
      responses:
        "201":
          description: Created category.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Category" } }

  # ---- AI Engine (Milestone 4) --------------------------------------------
  /ai/recipes/generate:
    post:
      tags: [AI]
      operationId: aiGenerateRecipe
      summary: Generate a Nigerian recipe from a brief
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiRecipeGenerationRequest" }
      responses:
        "200":
          description: A generated recipe with provenance metadata.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiStructuredResult" } }
        "401": { $ref: "#/components/responses/Error" }
        "429": { $ref: "#/components/responses/Error" }
  /ai/recipes/improve:
    post:
      tags: [AI]
      operationId: aiImproveRecipe
      summary: Improve an existing recipe toward a goal
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiRecipeImprovementRequest" }
      responses:
        "200":
          description: The improved recipe.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiStructuredResult" } }
  /ai/recipes/leftovers:
    post:
      tags: [AI]
      operationId: aiLeftoverRecipe
      summary: Turn leftover ingredients into a recipe
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiLeftoverRequest" }
      responses:
        "200":
          description: A recipe built from the leftovers.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiStructuredResult" } }
  /ai/recipes/summarize:
    post:
      tags: [AI]
      operationId: aiSummarizeRecipe
      summary: Summarise a recipe
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiSummarizationRequest" }
      responses:
        "200":
          description: A short text summary.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiTextResult" } }
  /ai/recipes/translate:
    post:
      tags: [AI]
      operationId: aiTranslateRecipe
      summary: Translate recipe content
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiTranslationRequest" }
      responses:
        "200":
          description: The translated text.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiTextResult" } }
  /ai/foods/describe:
    post:
      tags: [AI]
      operationId: aiDescribeFood
      summary: Generate an appetising food description
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiFoodDescriptionRequest" }
      responses:
        "200":
          description: A short description.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiTextResult" } }
  /ai/assistant/chat:
    post:
      tags: [AI]
      operationId: aiChat
      summary: Send a message to the Smart Cooking Assistant
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiChatRequest" }
      responses:
        "200":
          description: The assistant reply plus the updated conversation.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiChatTurn" } }
        "404": { $ref: "#/components/responses/Error" }
  /ai/assistant/tips:
    post:
      tags: [AI]
      operationId: aiCookingTips
      summary: Generate cooking tips for a topic
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiCookingTipsRequest" }
      responses:
        "200":
          description: Cooking tips text.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiTextResult" } }
  /ai/assistant/substitute:
    post:
      tags: [AI]
      operationId: aiSubstitute
      summary: Suggest ingredient substitutions
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiSubstitutionRequest" }
      responses:
        "200":
          description: Substitution suggestions.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiStructuredResult" } }
  /ai/assistant/meals:
    post:
      tags: [AI]
      operationId: aiMealSuggestions
      summary: Suggest meal ideas
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AiMealSuggestionRequest" }
      responses:
        "200":
          description: Meal suggestions.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiStructuredResult" } }
  /ai/conversations:
    get:
      tags: [AI]
      operationId: aiListConversations
      summary: List the caller's chat conversations
      responses:
        "200":
          description: Paginated conversation summaries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/ConversationSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
  /ai/conversations/{id}:
    get:
      tags: [AI]
      operationId: aiGetConversation
      summary: Read a full conversation thread
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The conversation with its messages.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/Conversation" } }
        "404": { $ref: "#/components/responses/Error" }
    patch:
      tags: [AI]
      operationId: aiRenameConversation
      summary: Rename a conversation
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [title]
              properties: { title: { type: string, maxLength: 160 } }
      responses:
        "200":
          description: The renamed conversation summary.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/ConversationSummary" } }
    delete:
      tags: [AI]
      operationId: aiDeleteConversation
      summary: Delete a conversation
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "404": { $ref: "#/components/responses/Error" }
  /ai/usage:
    get:
      tags: [AI]
      operationId: aiUsage
      summary: The caller's AI usage & cost summary
      parameters:
        - { name: days, in: query, required: false, schema: { type: integer, default: 30 } }
      responses:
        "200":
          description: Usage summary.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/AiUsageSummary" } }
  /admin/ai/prompts:
    get:
      tags: [AI Admin]
      operationId: aiListPromptVersions
      summary: List a feature's prompt versions
      parameters:
        - { name: feature, in: query, required: true, schema: { $ref: "#/components/schemas/AiFeature" } }
      responses:
        "200":
          description: Prompt versions, newest first.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/PromptTemplate" } }
    post:
      tags: [AI Admin]
      operationId: aiCreatePromptVersion
      summary: Publish a new prompt version
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/PromptRequest" }
      responses:
        "201":
          description: The created prompt version.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/PromptTemplate" } }
  /admin/ai/prompts/{id}/activate:
    post:
      tags: [AI Admin]
      operationId: aiActivatePrompt
      summary: Activate an existing prompt version
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The activated prompt version.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/PromptTemplate" } }

  # ---- Nutrition, Health & Personalisation (Milestone 5) ------------------
  /nutrition/items:
    get:
      tags: [Nutrition]
      operationId: nutritionItems
      summary: Search the nutrition database
      security: []
      parameters:
        - { name: q, in: query, required: false, schema: { type: string } }
        - { name: category, in: query, required: false, schema: { type: string } }
      responses:
        "200":
          description: Paginated nutrition items.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/NutritionItem" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
  /nutrition/items/{id}:
    get:
      tags: [Nutrition]
      operationId: nutritionItem
      summary: A nutrition item's full panel
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The nutrition item.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/NutritionItem" } }
        "404": { $ref: "#/components/responses/Error" }
  /nutrition/calculate:
    post:
      tags: [Nutrition]
      operationId: nutritionCalculate
      summary: Ad-hoc BMI/BMR/TDEE/target calculation
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CalculationRequest" }
      responses:
        "200":
          description: The assessment.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/NutritionAssessment" } }
  /nutrition/analyse:
    post:
      tags: [Nutrition]
      operationId: nutritionAnalyse
      summary: Meal / recipe nutrition analysis
      security: []
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/AnalysisRequest" }
      responses:
        "200":
          description: Totals + per-item breakdown.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/MealAnalysis" } }
  /nutrition/profile:
    get:
      tags: [Nutrition]
      operationId: getHealthProfile
      summary: The caller's health profile (or null)
      responses:
        "200":
          description: The profile, or null if not set.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/HealthProfile" } }
    put:
      tags: [Nutrition]
      operationId: saveHealthProfile
      summary: Create or update the caller's health profile
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/HealthProfileRequest" }
      responses:
        "200":
          description: The saved profile.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/HealthProfile" } }
  /nutrition/assessment:
    get:
      tags: [Nutrition]
      operationId: nutritionAssessment
      summary: Assessment from the saved profile
      responses:
        "200":
          description: The assessment.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/NutritionAssessment" } }
        "422": { $ref: "#/components/responses/Error" }
  /nutrition/diary:
    get:
      tags: [Nutrition]
      operationId: nutritionDiaryDay
      summary: A day's diary, totals and targets
      parameters:
        - { name: date, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200":
          description: The day's tracking summary.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/DailyNutritionSummary" } }
    post:
      tags: [Nutrition]
      operationId: nutritionLogDiary
      summary: Log a food for a day + meal
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/DiaryEntryRequest" }
      responses:
        "201":
          description: The logged entry.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/DiaryEntry" } }
  /nutrition/diary/{id}:
    delete:
      tags: [Nutrition]
      operationId: nutritionDeleteDiary
      summary: Delete a diary entry
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "404": { $ref: "#/components/responses/Error" }
  /nutrition/meal-plans:
    get:
      tags: [Nutrition]
      operationId: nutritionMealPlans
      summary: List the caller's meal plans
      responses:
        "200":
          description: Paginated meal plans.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/MealPlan" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
    post:
      tags: [Nutrition]
      operationId: nutritionCreateMealPlan
      summary: Create a meal plan
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MealPlanRequest" }
      responses:
        "201":
          description: The created plan.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/MealPlan" } }
  /nutrition/meal-plans/{id}:
    get:
      tags: [Nutrition]
      operationId: nutritionMealPlan
      summary: A meal plan
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The plan.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/MealPlan" } }
        "404": { $ref: "#/components/responses/Error" }
    delete:
      tags: [Nutrition]
      operationId: nutritionDeleteMealPlan
      summary: Delete a meal plan
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "404": { $ref: "#/components/responses/Error" }
  /nutrition/meal-plans/{id}/adjust:
    post:
      tags: [Nutrition]
      operationId: nutritionAdjustMealPlan
      summary: Scale all portions in a plan
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [factor]
              properties: { factor: { type: number, minimum: 0.1, maximum: 10 } }
      responses:
        "200":
          description: The adjusted plan.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/MealPlan" } }
  /nutrition/meal-plans/{id}/shopping-list:
    get:
      tags: [Nutrition]
      operationId: nutritionShoppingList
      summary: Generate a shopping list from a plan
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The shopping list.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/ShoppingList" } }
  /nutrition/progress:
    get:
      tags: [Nutrition]
      operationId: nutritionProgress
      summary: The caller's weight history (newest first)
      responses:
        "200":
          description: Progress measurements.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/ProgressEntry" } }
    post:
      tags: [Nutrition]
      operationId: nutritionRecordProgress
      summary: Record a weight measurement
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ProgressRequest" }
      responses:
        "201":
          description: The recorded measurement.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/ProgressEntry" } }
  /nutrition/recommendations/meals:
    get:
      tags: [Nutrition]
      operationId: nutritionRecommendMeals
      summary: AI personalised meal recommendations
      responses:
        "200": { $ref: "#/components/responses/NutritionAdvice" }
        "422": { $ref: "#/components/responses/Error" }
  /nutrition/recommendations/suggestions:
    get:
      tags: [Nutrition]
      operationId: nutritionSuggestions
      summary: AI smart nutrition suggestions for today
      parameters:
        - { name: date, in: query, required: false, schema: { type: string, format: date } }
      responses:
        "200": { $ref: "#/components/responses/NutritionAdvice" }
  /nutrition/recommendations/diet-improvement:
    get:
      tags: [Nutrition]
      operationId: nutritionDietImprovement
      summary: AI diet improvement recommendations
      responses:
        "200": { $ref: "#/components/responses/NutritionAdvice" }
  /nutrition/recommendations/weekly-insights:
    get:
      tags: [Nutrition]
      operationId: nutritionWeeklyInsights
      summary: AI weekly nutrition & progress insights
      responses:
        "200": { $ref: "#/components/responses/NutritionAdvice" }
  /admin/nutrition/items:
    post:
      tags: [Nutrition Admin]
      operationId: adminCreateNutritionItem
      summary: Create a nutrition-database item
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NutritionItemRequest" }
      responses:
        "201":
          description: The created item.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/NutritionItem" } }
  /admin/nutrition/items/{id}:
    put:
      tags: [Nutrition Admin]
      operationId: adminUpdateNutritionItem
      summary: Update a nutrition-database item
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/NutritionItemRequest" }
      responses:
        "200":
          description: The updated item.
          content:
            application/json:
              schema:
                type: object
                properties: { data: { $ref: "#/components/schemas/NutritionItem" } }
    delete:
      tags: [Nutrition Admin]
      operationId: adminDeleteNutritionItem
      summary: Delete a nutrition-database item
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "204": { description: Deleted. }
        "404": { $ref: "#/components/responses/Error" }

  # ---- Restaurant, Vendor & Food Business Platform (Milestone 6) ----------
  /vendors:
    get:
      tags: [Marketplace]
      operationId: browseVendors
      summary: Browse/search verified vendors
      security: []
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: type, in: query, schema: { type: string, enum: [restaurant, market_vendor, home_kitchen, cloud_kitchen] } }
        - { name: category, in: query, schema: { type: string } }
        - { name: lat, in: query, schema: { type: number } }
        - { name: lng, in: query, schema: { type: number } }
        - { name: radius_km, in: query, schema: { type: number } }
        - { name: featured, in: query, schema: { type: boolean } }
        - { name: sort, in: query, schema: { type: string, enum: [rating, nearest, name, recent] } }
      responses:
        "200":
          description: Paginated vendor summaries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/VendorSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
    post:
      tags: [Marketplace]
      operationId: registerVendor
      summary: Register a vendor (owner)
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/VendorRequest" }
      responses:
        "201":
          description: The created vendor (starts pending).
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Vendor" } } }
  /vendors/{slug}:
    get:
      tags: [Marketplace]
      operationId: getVendor
      summary: Vendor storefront detail
      security: []
      parameters:
        - { name: slug, in: path, required: true, schema: { type: string } }
      responses:
        "200":
          description: The vendor.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Vendor" } } }
        "404": { $ref: "#/components/responses/Error" }
  /vendors/{vendorId}/menu:
    get:
      tags: [Marketplace]
      operationId: getVendorMenu
      summary: A vendor's menu items
      security: []
      parameters:
        - { name: vendorId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Menu items.
          content:
            application/json:
              schema: { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/MenuItem" } } } }
    post:
      tags: [Marketplace]
      operationId: createMenuItem
      summary: Add a menu item (owner)
      parameters:
        - { name: vendorId, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/MenuItemRequest" }
      responses:
        "201":
          description: The created item.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/MenuItem" } } }
  /vendors/{id}/reviews:
    get:
      tags: [Marketplace]
      operationId: vendorReviews
      summary: List a vendor's reviews
      security: []
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Paginated reviews.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/VendorReview" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
    post:
      tags: [Marketplace]
      operationId: reviewVendor
      summary: Rate & review a vendor
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [rating]
              properties:
                rating: { type: integer, minimum: 1, maximum: 5 }
                comment: { type: string, nullable: true }
      responses:
        "201":
          description: The review.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/VendorReview" } } }
  /search/menu-items:
    get:
      tags: [Marketplace]
      operationId: searchMenuItems
      summary: Search available menu items
      security: []
      parameters:
        - { name: q, in: query, schema: { type: string } }
        - { name: vendor_id, in: query, schema: { type: string, format: uuid } }
        - { name: featured, in: query, schema: { type: boolean } }
      responses:
        "200":
          description: Paginated menu items.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/MenuItem" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
  /me/vendors:
    get:
      tags: [Marketplace]
      operationId: myVendors
      summary: The caller's vendors
      responses:
        "200":
          description: Owned vendors.
          content:
            application/json:
              schema: { type: object, properties: { data: { type: array, items: { $ref: "#/components/schemas/Vendor" } } } }
  /vendors/{id}/dashboard:
    get:
      tags: [Marketplace]
      operationId: vendorDashboard
      summary: Vendor sales dashboard (owner)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: Sales summary.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/SalesSummary" } } }
  /menu-items/{itemId}/describe:
    post:
      tags: [Marketplace]
      operationId: describeMenuItem
      summary: AI-generate a menu item description (owner)
      parameters:
        - { name: itemId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The item with a generated description.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/MenuItem" } } }
  /cart:
    get:
      tags: [Marketplace]
      operationId: getCart
      summary: The caller's cart
      responses:
        "200":
          description: The cart.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Cart" } } }
    delete:
      tags: [Marketplace]
      operationId: clearCart
      summary: Clear the cart
      responses:
        "204": { description: Cleared. }
  /cart/items:
    post:
      tags: [Marketplace]
      operationId: addCartItem
      summary: Add an item to the cart
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CartItemRequest" }
      responses:
        "201":
          description: The updated cart.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Cart" } } }
    put:
      tags: [Marketplace]
      operationId: updateCartItem
      summary: Change a cart item's quantity
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CartItemRequest" }
      responses:
        "200":
          description: The updated cart.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Cart" } } }
  /checkout:
    post:
      tags: [Marketplace]
      operationId: checkout
      summary: Check out the cart into an order
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/CheckoutRequest" }
      responses:
        "201":
          description: The placed order.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Order" } } }
        "422": { $ref: "#/components/responses/Error" }
  /orders:
    get:
      tags: [Marketplace]
      operationId: orderHistory
      summary: The caller's order history
      responses:
        "200":
          description: Paginated order summaries.
          content:
            application/json:
              schema:
                type: object
                properties:
                  data: { type: array, items: { $ref: "#/components/schemas/OrderSummary" } }
                  meta: { $ref: "#/components/schemas/PageMeta" }
  /orders/{id}:
    get:
      tags: [Marketplace]
      operationId: getOrder
      summary: Order detail & tracking
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The order.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Order" } } }
        "404": { $ref: "#/components/responses/Error" }
  /orders/{id}/status:
    post:
      tags: [Marketplace]
      operationId: advanceOrder
      summary: Advance order status (owning vendor)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties: { status: { type: string, enum: [confirmed, preparing, ready, dispatched, delivered] } }
      responses:
        "200":
          description: The updated order.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Order" } } }
  /orders/{id}/cancel:
    post:
      tags: [Marketplace]
      operationId: cancelOrder
      summary: Cancel an order (customer, owning vendor or admin)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The cancelled order.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Order" } } }
  /orders/{orderId}/delivery:
    get:
      tags: [Marketplace]
      operationId: getOrderDelivery
      summary: The delivery for an order
      parameters:
        - { name: orderId, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The delivery.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Delivery" } } }
  /deliveries/{id}/assign:
    post:
      tags: [Marketplace]
      operationId: assignRider
      summary: Assign a rider (owning vendor)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { type: object, required: [rider_id], properties: { rider_id: { type: string, format: uuid } } }
      responses:
        "200":
          description: The delivery.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Delivery" } } }
  /deliveries/{id}/status:
    post:
      tags: [Marketplace]
      operationId: advanceDelivery
      summary: Progress delivery status (assigned rider)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties: { status: { type: string, enum: [picked_up, en_route, delivered, failed] } }
      responses:
        "200":
          description: The delivery.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Delivery" } } }
  /deliveries/{id}/track:
    post:
      tags: [Marketplace]
      operationId: trackDelivery
      summary: Append a live-tracking point (assigned rider)
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/GeoPoint" }
      responses:
        "200":
          description: The delivery.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Delivery" } } }
  /riders:
    post:
      tags: [Marketplace]
      operationId: onboardRider
      summary: Onboard as a rider
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [name, phone, vehicle_type]
              properties:
                name: { type: string }
                phone: { type: string }
                vehicle_type: { type: string, enum: [bicycle, motorbike, car, foot] }
      responses:
        "201":
          description: The rider.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Rider" } } }
  /riders/me:
    get:
      tags: [Marketplace]
      operationId: getRider
      summary: The caller's rider profile
      responses:
        "200":
          description: The rider.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Rider" } } }
  /admin/marketplace/vendors/{id}/verify:
    post:
      tags: [Marketplace Admin]
      operationId: verifyVendor
      summary: Verify a vendor
      parameters:
        - { name: id, in: path, required: true, schema: { type: string, format: uuid } }
      responses:
        "200":
          description: The verified vendor.
          content:
            application/json:
              schema: { type: object, properties: { data: { $ref: "#/components/schemas/Vendor" } } }


components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT

  responses:
    Error:
      description: Error envelope.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Message:
      description: Simple message envelope.
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: object
                properties:
                  message: { type: string }
    User:
      description: A user resource.
      content:
        application/json:
          schema:
            type: object
            properties:
              data: { $ref: "#/components/schemas/User" }
    AuthSuccess:
      description: Authenticated — user + tokens.
      content:
        application/json:
          schema:
            type: object
            properties:
              data: { $ref: "#/components/schemas/AuthPayload" }
    AuthSuccessOrChallenge:
      description: Either authenticated (user + tokens) or a two-factor challenge.
      content:
        application/json:
          schema:
            oneOf:
              - type: object
                properties:
                  data: { $ref: "#/components/schemas/AuthPayload" }
              - type: object
                properties:
                  data: { $ref: "#/components/schemas/TwoFactorChallenge" }
    NutritionAdvice:
      description: AI personalisation advice + provenance.
      content:
        application/json:
          schema:
            type: object
            properties:
              data: { $ref: "#/components/schemas/NutritionAdvicePayload" }

  schemas:
    SystemStatus:
      type: object
      required: [status, service, version, environment, timestamp]
      properties:
        status: { type: string, enum: [ok, degraded] }
        service: { type: string }
        version: { type: string }
        environment: { type: string }
        timestamp: { type: string, format: date-time }

    RegisterRequest:
      type: object
      required: [name, email, password, password_confirmation]
      properties:
        name: { type: string, minLength: 2, maxLength: 120 }
        email: { type: string, format: email }
        password: { type: string, minLength: 8 }
        password_confirmation: { type: string }

    LoginRequest:
      type: object
      required: [email, password]
      properties:
        email: { type: string, format: email }
        password: { type: string }

    ResetPasswordRequest:
      type: object
      required: [email, token, password, password_confirmation]
      properties:
        email: { type: string, format: email }
        token: { type: string }
        password: { type: string, minLength: 8 }
        password_confirmation: { type: string }

    Tokens:
      type: object
      properties:
        access_token: { type: string }
        token_type: { type: string, example: Bearer }
        expires_in: { type: integer, description: Access-token lifetime in seconds. }
        refresh_token: { type: string }

    AuthPayload:
      type: object
      properties:
        user: { $ref: "#/components/schemas/User" }
        tokens: { $ref: "#/components/schemas/Tokens" }

    TwoFactorChallenge:
      type: object
      properties:
        two_factor_required: { type: boolean, example: true }
        challenge_token: { type: string }

    TwoFactorEnrollment:
      type: object
      properties:
        secret: { type: string }
        provisioning_uri: { type: string, description: otpauth:// URI for QR codes. }
        recovery_codes:
          type: array
          items: { type: string }

    User:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        email: { type: string, format: email }
        phone: { type: string, nullable: true }
        email_verified: { type: boolean }
        avatar_url: { type: string, nullable: true }
        roles:
          type: array
          items: { type: string, enum: [admin, moderator, user] }
        permissions:
          type: array
          items: { type: string }
        preferences: { type: object, additionalProperties: true }
        two_factor_enabled: { type: boolean }
        status: { type: string, enum: [active, suspended] }

    Session:
      type: object
      properties:
        session_id: { type: string, format: uuid }
        ip_address: { type: string, nullable: true }
        user_agent: { type: string, nullable: true }
        created_at: { type: string, format: date-time }
        last_used_at: { type: string, format: date-time, nullable: true }

    PageMeta:
      type: object
      properties:
        page: { type: integer }
        per_page: { type: integer }
        total: { type: integer }

    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code: { type: string, example: INVALID_CREDENTIALS }
            message: { type: string }
            details:
              type: array
              items: { type: object }

    Category:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        type: { type: string, enum: [soup, swallow, rice, protein, snack, street_food, drink, dessert, breakfast, side_dish] }
        description: { type: string, nullable: true }
        sort_order: { type: integer }
        active: { type: boolean }

    LocalName:
      type: object
      properties:
        name: { type: string, example: Iyan }
        language: { type: string, example: yoruba }

    Nutrition:
      type: object
      properties:
        calories: { type: integer }
        protein_grams: { type: number }
        carbohydrate_grams: { type: number }
        fat_grams: { type: number }
        fiber_grams: { type: number }
        basis: { type: string, example: per_serving }

    Ingredient:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        description: { type: string, nullable: true }
        local_names: { type: array, items: { $ref: "#/components/schemas/LocalName" } }
        nutrition_per_100g: { $ref: "#/components/schemas/Nutrition" }

    FoodSummary:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        category_id: { type: string, format: uuid }
        region: { type: string }
        tags: { type: array, items: { type: string } }
        status: { type: string, enum: [draft, published, archived] }
        primary_image: { type: string, nullable: true }

    Food:
      allOf:
        - $ref: "#/components/schemas/FoodSummary"
        - type: object
          properties:
            description: { type: string, nullable: true }
            states: { type: array, items: { type: string } }
            local_names: { type: array, items: { $ref: "#/components/schemas/LocalName" } }
            nutrition: { $ref: "#/components/schemas/Nutrition" }
            images: { type: array, items: { type: string } }
            video_url: { type: string, nullable: true }

    FoodInput:
      type: object
      required: [name, category_id, region]
      properties:
        name: { type: string }
        category_id: { type: string, format: uuid }
        region: { type: string, enum: [north_central, north_east, north_west, south_east, south_south, south_west, nationwide] }
        description: { type: string, nullable: true }
        states: { type: array, items: { type: string } }
        local_names: { type: array, items: { $ref: "#/components/schemas/LocalName" } }
        nutrition: { $ref: "#/components/schemas/Nutrition" }
        tags: { type: array, items: { type: string } }

    RecipeIngredientInput:
      type: object
      required: [name, amount, unit]
      properties:
        name: { type: string }
        amount: { type: number }
        unit: { type: string, enum: [g, kg, ml, l, cup, tbsp, tsp, piece, pinch, handful, wrap, to_taste] }
        ingredient_id: { type: string, format: uuid, nullable: true }
        note: { type: string, nullable: true }

    CookingStepInput:
      type: object
      required: [order, instruction]
      properties:
        order: { type: integer }
        instruction: { type: string }
        duration_minutes: { type: integer, nullable: true }

    RecipeInput:
      type: object
      required: [food_id, title, prep_time_minutes, cook_time_minutes, difficulty, serving_size, ingredients, steps]
      properties:
        food_id: { type: string, format: uuid }
        title: { type: string }
        summary: { type: string, nullable: true }
        prep_time_minutes: { type: integer }
        cook_time_minutes: { type: integer }
        difficulty: { type: string, enum: [easy, medium, hard] }
        serving_size: { type: integer }
        ingredients: { type: array, items: { $ref: "#/components/schemas/RecipeIngredientInput" } }
        steps: { type: array, items: { $ref: "#/components/schemas/CookingStepInput" } }
        tips: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }

    RecipeSummary:
      type: object
      properties:
        id: { type: string, format: uuid }
        food_id: { type: string, format: uuid }
        title: { type: string }
        slug: { type: string }
        summary: { type: string, nullable: true }
        difficulty: { type: string, enum: [easy, medium, hard] }
        prep_time_minutes: { type: integer }
        cook_time_minutes: { type: integer }
        total_time_minutes: { type: integer }
        serving_size: { type: integer }
        rating_average: { type: number }
        rating_count: { type: integer }
        tags: { type: array, items: { type: string } }
        status: { type: string, enum: [draft, published, archived] }

    Recipe:
      allOf:
        - $ref: "#/components/schemas/RecipeSummary"
        - type: object
          properties:
            author_id: { type: string, format: uuid }
            version: { type: integer }
            ingredients: { type: array, items: { $ref: "#/components/schemas/RecipeIngredientInput" } }
            steps:
              type: array
              items:
                type: object
                properties:
                  order: { type: integer }
                  instruction: { type: string }
                  image_url: { type: string, nullable: true }
                  duration_minutes: { type: integer, nullable: true }
            tips: { type: array, items: { type: string } }
            related_recipe_ids: { type: array, items: { type: string, format: uuid } }
            is_favourited: { type: boolean }

    RecipeReview:
      type: object
      properties:
        id: { type: string, format: uuid }
        recipe_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string, nullable: true }
        created_at: { type: string, format: date-time }

    # ---- AI Engine schemas (Milestone 4) ----
    AiFeature:
      type: string
      enum:
        - recipe_generation
        - recipe_improvement
        - recipe_translation
        - ingredient_substitution
        - cooking_assistant
        - meal_suggestions
        - leftover_recipes
        - recipe_summarization
        - cooking_tips
        - food_description

    AiMeta:
      type: object
      description: Provenance for a generation (provider, model, cache, tokens).
      properties:
        provider: { type: string, enum: [anthropic, openai, gemini, local, mock] }
        model: { type: string }
        cached: { type: boolean }
        finish_reason: { type: string }
        tokens:
          type: object
          properties:
            input: { type: integer }
            output: { type: integer }
            total: { type: integer }

    AiStructuredResult:
      type: object
      description: A structured (JSON) generation result plus its metadata.
      properties:
        content: { type: object, additionalProperties: true }
        meta: { $ref: "#/components/schemas/AiMeta" }

    AiTextResult:
      type: object
      description: A plain-text generation result plus its metadata.
      properties:
        content: { type: string }
        meta: { $ref: "#/components/schemas/AiMeta" }

    AiRecipeGenerationRequest:
      type: object
      required: [dish_name]
      properties:
        dish_name: { type: string, minLength: 2, maxLength: 160 }
        servings: { type: integer, minimum: 1, maximum: 50 }
        difficulty: { type: string, enum: [easy, medium, hard] }
        dietary_preferences: { type: array, items: { type: string } }
        available_ingredients: { type: array, items: { type: string } }
        notes: { type: string, nullable: true }

    AiRecipeImprovementRequest:
      type: object
      required: [title, ingredients, steps]
      properties:
        title: { type: string }
        ingredients: { type: array, items: { type: string } }
        steps: { type: array, items: { type: string } }
        goal: { type: string, nullable: true }

    AiLeftoverRequest:
      type: object
      required: [ingredients]
      properties:
        ingredients: { type: array, items: { type: string } }
        dietary_preferences: { type: array, items: { type: string } }
        meal_type: { type: string, nullable: true }

    AiSummarizationRequest:
      type: object
      required: [content]
      properties:
        content: { type: string, maxLength: 8000 }
        max_words: { type: integer, minimum: 10, maximum: 400 }

    AiTranslationRequest:
      type: object
      required: [content, target_language]
      properties:
        content: { type: string, maxLength: 8000 }
        target_language: { type: string }

    AiFoodDescriptionRequest:
      type: object
      required: [food_name]
      properties:
        food_name: { type: string, minLength: 2, maxLength: 160 }
        region: { type: string, nullable: true }
        keywords: { type: array, items: { type: string } }

    AiCookingTipsRequest:
      type: object
      required: [topic]
      properties:
        topic: { type: string, minLength: 2, maxLength: 160 }
        skill_level: { type: string, enum: [beginner, "home cook", intermediate, advanced] }

    AiSubstitutionRequest:
      type: object
      required: [ingredient]
      properties:
        ingredient: { type: string }
        reason: { type: string, nullable: true }
        dish_context: { type: string, nullable: true }
        dietary_preferences: { type: array, items: { type: string } }

    AiMealSuggestionRequest:
      type: object
      properties:
        meal_type: { type: string }
        dietary_preferences: { type: array, items: { type: string } }
        count: { type: integer, minimum: 1, maximum: 10 }
        budget: { type: string, nullable: true }

    AiChatRequest:
      type: object
      required: [message]
      properties:
        message: { type: string, minLength: 1, maxLength: 4000 }
        conversation_id: { type: string, format: uuid, nullable: true }

    ChatMessage:
      type: object
      properties:
        role: { type: string, enum: [system, user, assistant] }
        content: { type: string }
        created_at: { type: string, format: date-time }

    ConversationSummary:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        feature: { $ref: "#/components/schemas/AiFeature" }
        message_count: { type: integer }
        created_at: { type: string, format: date-time }
        updated_at: { type: string, format: date-time }

    Conversation:
      allOf:
        - $ref: "#/components/schemas/ConversationSummary"
        - type: object
          properties:
            messages: { type: array, items: { $ref: "#/components/schemas/ChatMessage" } }

    AiChatTurn:
      type: object
      properties:
        conversation_id: { type: string, format: uuid }
        reply: { type: string }
        conversation: { $ref: "#/components/schemas/Conversation" }
        meta: { $ref: "#/components/schemas/AiMeta" }

    AiUsageSummary:
      type: object
      properties:
        requests: { type: integer }
        cached_requests: { type: integer }
        input_tokens: { type: integer }
        output_tokens: { type: integer }
        total_tokens: { type: integer }
        cost_usd: { type: number }

    PromptTemplate:
      type: object
      properties:
        id: { type: string, format: uuid }
        feature: { $ref: "#/components/schemas/AiFeature" }
        version: { type: integer }
        name: { type: string }
        system_template: { type: string }
        user_template: { type: string }
        model: { type: string, nullable: true }
        variables: { type: array, items: { type: string } }
        active: { type: boolean }
        created_at: { type: string, format: date-time }

    PromptRequest:
      type: object
      required: [feature, name, user_template]
      properties:
        feature: { $ref: "#/components/schemas/AiFeature" }
        name: { type: string, maxLength: 160 }
        system_template: { type: string, nullable: true }
        user_template: { type: string }
        model: { type: string, nullable: true }
        variables: { type: array, items: { type: string } }
        activate: { type: boolean, default: true }

    # ---- Nutrition schemas (Milestone 5) ----
    NutritionFacts:
      type: object
      properties:
        calories: { type: number }
        protein_grams: { type: number }
        carb_grams: { type: number }
        fat_grams: { type: number }
        fibre_grams: { type: number }
        sugar_grams: { type: number }
        sodium_mg: { type: number }
        cholesterol_mg: { type: number }
        water_ml: { type: number }
        micronutrients: { type: object, additionalProperties: { type: number } }

    NutritionFactsInput:
      type: object
      required: [calories, protein_grams, carb_grams, fat_grams]
      properties:
        calories: { type: number, minimum: 0 }
        protein_grams: { type: number, minimum: 0 }
        carb_grams: { type: number, minimum: 0 }
        fat_grams: { type: number, minimum: 0 }
        fibre_grams: { type: number, minimum: 0 }
        sugar_grams: { type: number, minimum: 0 }
        sodium_mg: { type: number, minimum: 0 }
        cholesterol_mg: { type: number, minimum: 0 }
        water_ml: { type: number, minimum: 0 }
        micronutrients: { type: object, additionalProperties: { type: number } }

    NutritionItem:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        category: { type: string }
        serving_size:
          type: object
          properties: { label: { type: string }, grams: { type: number } }
        nutrition: { $ref: "#/components/schemas/NutritionFacts" }
        food_id: { type: string, format: uuid, nullable: true }

    NutritionItemRequest:
      type: object
      required: [name, serving_size, nutrition]
      properties:
        name: { type: string, minLength: 2, maxLength: 160 }
        category: { type: string }
        serving_size:
          type: object
          required: [label, grams]
          properties: { label: { type: string }, grams: { type: number, minimum: 0.1 } }
        nutrition: { $ref: "#/components/schemas/NutritionFactsInput" }
        food_id: { type: string, format: uuid, nullable: true }

    MacroTargets:
      type: object
      properties:
        protein_grams: { type: integer }
        carb_grams: { type: integer }
        fat_grams: { type: integer }

    NutritionAssessment:
      type: object
      properties:
        bmi: { type: number }
        bmi_category: { type: string, enum: [underweight, normal, overweight, obese] }
        bmr: { type: integer }
        tdee: { type: integer }
        calorie_target: { type: integer }
        macro_targets: { $ref: "#/components/schemas/MacroTargets" }

    CalculationRequest:
      type: object
      required: [weight_kg, height_cm, age, gender]
      properties:
        weight_kg: { type: number, minimum: 20, maximum: 500 }
        height_cm: { type: number, minimum: 50, maximum: 260 }
        age: { type: integer, minimum: 1, maximum: 120 }
        gender: { type: string, enum: [male, female, other] }
        activity_level: { type: string, enum: [sedentary, light, moderate, active, very_active] }
        goal: { type: string, enum: [lose_weight, maintain, gain_weight, gain_muscle] }

    HealthProfile:
      type: object
      nullable: true
      properties:
        user_id: { type: string, format: uuid }
        weight_kg: { type: number }
        height_cm: { type: number }
        age: { type: integer }
        gender: { type: string, enum: [male, female, other] }
        activity_level: { type: string, enum: [sedentary, light, moderate, active, very_active] }
        goal: { type: string, enum: [lose_weight, maintain, gain_weight, gain_muscle] }
        dietary_preferences: { type: array, items: { type: string } }
        allergies: { type: array, items: { type: string } }
        medical_restrictions: { type: array, items: { type: string } }
        updated_at: { type: string, format: date-time }

    HealthProfileRequest:
      type: object
      required: [weight_kg, height_cm, age, gender, activity_level, goal]
      properties:
        weight_kg: { type: number, minimum: 20, maximum: 500 }
        height_cm: { type: number, minimum: 50, maximum: 260 }
        age: { type: integer, minimum: 1, maximum: 120 }
        gender: { type: string, enum: [male, female, other] }
        activity_level: { type: string, enum: [sedentary, light, moderate, active, very_active] }
        goal: { type: string, enum: [lose_weight, maintain, gain_weight, gain_muscle] }
        dietary_preferences: { type: array, items: { type: string } }
        allergies: { type: array, items: { type: string } }
        medical_restrictions: { type: array, items: { type: string } }

    DiaryEntry:
      type: object
      properties:
        id: { type: string, format: uuid }
        date: { type: string, format: date }
        meal_type: { type: string, enum: [breakfast, lunch, dinner, snack] }
        item_name: { type: string }
        servings: { type: number }
        nutrition_item_id: { type: string, format: uuid, nullable: true }
        nutrition: { $ref: "#/components/schemas/NutritionFacts" }

    DiaryEntryRequest:
      type: object
      required: [date, meal_type, servings]
      properties:
        date: { type: string, format: date }
        meal_type: { type: string, enum: [breakfast, lunch, dinner, snack] }
        servings: { type: number, minimum: 0.1, maximum: 100 }
        nutrition_item_id: { type: string, format: uuid, nullable: true }
        item_name: { type: string, nullable: true }
        nutrition: { $ref: "#/components/schemas/NutritionFactsInput" }

    DailyNutritionSummary:
      type: object
      properties:
        date: { type: string, format: date }
        entries: { type: array, items: { $ref: "#/components/schemas/DiaryEntry" } }
        totals: { $ref: "#/components/schemas/NutritionFacts" }
        targets:
          oneOf:
            - { $ref: "#/components/schemas/NutritionAssessment" }
            - { type: "null" }
        remaining_calories: { type: integer, nullable: true }

    MealAnalysis:
      type: object
      properties:
        totals: { $ref: "#/components/schemas/NutritionFacts" }
        items:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              servings: { type: number }
              facts: { $ref: "#/components/schemas/NutritionFacts" }

    AnalysisRequest:
      type: object
      required: [items]
      properties:
        items:
          type: array
          minItems: 1
          items:
            type: object
            required: [nutrition_item_id, servings]
            properties:
              nutrition_item_id: { type: string, format: uuid }
              servings: { type: number, minimum: 0.1, maximum: 100 }

    MealPlanEntry:
      type: object
      properties:
        date: { type: string, format: date }
        meal_type: { type: string, enum: [breakfast, lunch, dinner, snack] }
        label: { type: string }
        servings: { type: number }
        nutrition_item_id: { type: string, format: uuid, nullable: true }
        estimated_cost: { type: number, nullable: true }

    MealPlan:
      type: object
      properties:
        id: { type: string, format: uuid }
        title: { type: string }
        period: { type: string, enum: [daily, weekly, monthly] }
        start_date: { type: string, format: date }
        entries: { type: array, items: { $ref: "#/components/schemas/MealPlanEntry" } }
        estimated_cost: { type: number }
        created_at: { type: string, format: date-time }

    MealPlanRequest:
      type: object
      required: [title, period, start_date]
      properties:
        title: { type: string, minLength: 2, maxLength: 160 }
        period: { type: string, enum: [daily, weekly, monthly] }
        start_date: { type: string, format: date }
        entries:
          type: array
          items:
            type: object
            required: [date, meal_type, label, servings]
            properties:
              date: { type: string, format: date }
              meal_type: { type: string, enum: [breakfast, lunch, dinner, snack] }
              label: { type: string }
              servings: { type: number, minimum: 0.1, maximum: 100 }
              nutrition_item_id: { type: string, format: uuid, nullable: true }
              estimated_cost: { type: number, nullable: true }

    ShoppingList:
      type: object
      properties:
        items:
          type: array
          items:
            type: object
            properties:
              name: { type: string }
              servings: { type: number }
              estimated_cost: { type: number, nullable: true }
        total_estimated_cost: { type: number }

    ProgressEntry:
      type: object
      properties:
        id: { type: string, format: uuid }
        date: { type: string, format: date }
        weight_kg: { type: number }
        note: { type: string, nullable: true }

    ProgressRequest:
      type: object
      required: [date, weight_kg]
      properties:
        date: { type: string, format: date }
        weight_kg: { type: number, minimum: 20, maximum: 500 }
        note: { type: string, nullable: true }

    NutritionAdvicePayload:
      type: object
      properties:
        advice: { type: string }
        meta:
          type: object
          properties:
            provider: { type: string }
            model: { type: string }
            cached: { type: boolean }

    # ---- Marketplace schemas (Milestone 6) ----
    GeoPoint:
      type: object
      required: [latitude, longitude]
      properties:
        latitude: { type: number, minimum: -90, maximum: 90 }
        longitude: { type: number, minimum: -180, maximum: 180 }

    VendorSummary:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        slug: { type: string }
        type: { type: string, enum: [restaurant, market_vendor, home_kitchen, cloud_kitchen] }
        category: { type: string }
        status: { type: string, enum: [pending, verified, suspended, rejected] }
        rating_average: { type: number }
        rating_count: { type: integer }
        featured: { type: boolean }
        location:
          oneOf:
            - { $ref: "#/components/schemas/GeoPoint" }
            - { type: "null" }
        primary_image: { type: string, nullable: true }

    Vendor:
      allOf:
        - $ref: "#/components/schemas/VendorSummary"
        - type: object
          properties:
            owner_user_id: { type: string, format: uuid }
            description: { type: string, nullable: true }
            contact:
              type: object
              properties: { phone: { type: string }, email: { type: string, nullable: true }, whatsapp: { type: string, nullable: true } }
            address:
              type: object
              properties:
                line: { type: string }
                city: { type: string }
                state: { type: string }
                location:
                  oneOf:
                    - { $ref: "#/components/schemas/GeoPoint" }
                    - { type: "null" }
            branches: { type: array, items: { type: object } }
            business_hours: { type: object, additionalProperties: { type: object, properties: { open: { type: string }, close: { type: string } } } }
            delivery_zones:
              type: array
              items:
                type: object
                properties: { name: { type: string }, fee_minor: { type: integer }, radius_km: { type: number } }
            images: { type: array, items: { type: string } }
            created_at: { type: string, format: date-time }

    VendorRequest:
      type: object
      required: [name, type, category, contact, address]
      properties:
        name: { type: string, minLength: 2, maxLength: 160 }
        type: { type: string, enum: [restaurant, market_vendor, home_kitchen, cloud_kitchen] }
        category: { type: string }
        description: { type: string, nullable: true }
        contact:
          type: object
          required: [phone]
          properties: { phone: { type: string }, email: { type: string, nullable: true }, whatsapp: { type: string, nullable: true } }
        address:
          type: object
          required: [line, city, state]
          properties:
            line: { type: string }
            city: { type: string }
            state: { type: string }
            location: { $ref: "#/components/schemas/GeoPoint" }

    MenuVariant:
      type: object
      properties:
        name: { type: string }
        price_delta_minor: { type: integer }

    MenuItem:
      type: object
      properties:
        id: { type: string, format: uuid }
        vendor_id: { type: string, format: uuid }
        category_id: { type: string, format: uuid, nullable: true }
        name: { type: string }
        description: { type: string, nullable: true }
        description_ai_generated: { type: boolean }
        base_price_minor: { type: integer }
        currency: { type: string }
        variants: { type: array, items: { $ref: "#/components/schemas/MenuVariant" } }
        available: { type: boolean }
        orderable: { type: boolean }
        images: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }
        featured: { type: boolean }
        promotion:
          oneOf:
            - { type: object, properties: { type: { type: string }, value: { type: integer } } }
            - { type: "null" }
        tracks_inventory: { type: boolean }
        stock: { type: integer, nullable: true }
        calories: { type: integer, nullable: true }
        nutrition_item_id: { type: string, format: uuid, nullable: true }

    MenuItemRequest:
      type: object
      required: [name, base_price_minor]
      properties:
        category_id: { type: string, format: uuid, nullable: true }
        name: { type: string, minLength: 2, maxLength: 160 }
        description: { type: string, nullable: true }
        base_price_minor: { type: integer, minimum: 0 }
        variants: { type: array, items: { type: object, properties: { name: { type: string }, price_delta_minor: { type: integer } } } }
        images: { type: array, items: { type: string } }
        tags: { type: array, items: { type: string } }
        track_inventory: { type: boolean }
        stock: { type: integer, minimum: 0 }
        calories: { type: integer, nullable: true }
        nutrition_item_id: { type: string, format: uuid, nullable: true }

    CartLine:
      type: object
      properties:
        menu_item_id: { type: string, format: uuid }
        name: { type: string }
        variant_name: { type: string, nullable: true }
        unit_price_minor: { type: integer }
        quantity: { type: integer }
        line_total_minor: { type: integer }

    Cart:
      type: object
      properties:
        vendor_id: { type: string, format: uuid, nullable: true }
        currency: { type: string }
        items: { type: array, items: { $ref: "#/components/schemas/CartLine" } }
        subtotal_minor: { type: integer }

    CartItemRequest:
      type: object
      required: [menu_item_id, quantity]
      properties:
        menu_item_id: { type: string, format: uuid }
        variant_name: { type: string, nullable: true }
        quantity: { type: integer, minimum: 1, maximum: 50 }

    CheckoutRequest:
      type: object
      required: [fulfilment]
      properties:
        fulfilment: { type: string, enum: [delivery, pickup] }
        delivery_address:
          type: object
          properties:
            line: { type: string }
            city: { type: string }
            state: { type: string }
            location: { $ref: "#/components/schemas/GeoPoint" }
        scheduled_for: { type: string, format: date-time, nullable: true }
        note: { type: string, nullable: true }

    OrderSummary:
      type: object
      properties:
        id: { type: string, format: uuid }
        reference: { type: string }
        vendor_id: { type: string, format: uuid }
        status: { type: string, enum: [pending, confirmed, preparing, ready, dispatched, delivered, cancelled] }
        fulfilment: { type: string, enum: [delivery, pickup] }
        total_minor: { type: integer }
        currency: { type: string }
        placed_at: { type: string, format: date-time }

    Order:
      allOf:
        - $ref: "#/components/schemas/OrderSummary"
        - type: object
          properties:
            customer_user_id: { type: string, format: uuid }
            lines: { type: array, items: { $ref: "#/components/schemas/CartLine" } }
            subtotal_minor: { type: integer }
            delivery_fee_minor: { type: integer }
            delivery_address: { type: object, nullable: true }
            scheduled_for: { type: string, format: date-time, nullable: true }
            note: { type: string, nullable: true }
            status_history:
              type: array
              items: { type: object, properties: { status: { type: string }, at: { type: string, format: date-time } } }

    Delivery:
      type: object
      properties:
        id: { type: string, format: uuid }
        order_id: { type: string, format: uuid }
        rider_id: { type: string, format: uuid, nullable: true }
        status: { type: string, enum: [unassigned, assigned, picked_up, en_route, delivered, failed] }
        fee_minor: { type: integer }
        zone_name: { type: string, nullable: true }
        pickup:
          oneOf: [ { $ref: "#/components/schemas/GeoPoint" }, { type: "null" } ]
        dropoff:
          oneOf: [ { $ref: "#/components/schemas/GeoPoint" }, { type: "null" } ]
        track_points:
          type: array
          items: { type: object, properties: { lat: { type: number }, lng: { type: number }, at: { type: string, format: date-time } } }
        assigned_at: { type: string, format: date-time, nullable: true }
        delivered_at: { type: string, format: date-time, nullable: true }

    Rider:
      type: object
      properties:
        id: { type: string, format: uuid }
        name: { type: string }
        phone: { type: string }
        vehicle_type: { type: string, enum: [bicycle, motorbike, car, foot] }
        status: { type: string, enum: [available, busy, offline] }
        location:
          oneOf: [ { $ref: "#/components/schemas/GeoPoint" }, { type: "null" } ]

    VendorReview:
      type: object
      properties:
        id: { type: string, format: uuid }
        vendor_id: { type: string, format: uuid }
        user_id: { type: string, format: uuid }
        rating: { type: integer, minimum: 1, maximum: 5 }
        comment: { type: string, nullable: true }
        created_at: { type: string, format: date-time }

    SalesSummary:
      type: object
      properties:
        total_orders: { type: integer }
        delivered_orders: { type: integer }
        pending_orders: { type: integer }
        revenue_minor: { type: integer }
        currency: { type: string }
