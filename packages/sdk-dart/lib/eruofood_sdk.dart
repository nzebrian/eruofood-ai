/// EruoFood Public API — Dart SDK (foundation).
///
/// A minimal client over the public API: API-key auth, configuration, a typed
/// error, and a pagination helper. Depends only on `package:http`.
library eruofood_sdk;

export 'src/client.dart';
export 'src/exceptions.dart';
export 'src/page.dart';
