# =============================================================================
# EruoFood AI — Mobile runtime config. Copy to .env (bundled as an asset).
# Do not put secrets here; the app ships to devices. Public config only.
# =============================================================================
API_BASE_URL=http://10.0.2.2:8080/api/v1
APP_ENV=local
APP_NAME=EruoFood AI
